# Problem 4: Three ways to sum to n

Short description

- Implements three approaches to compute the sum `1 + 2 + ... + n`:
  - Arithmetic Formula (O(1)), Recursion (O(n) time, O(n) space), Iteration (O(n) time, O(1) space)

Setup

- `cd problem-4`
- `pnpm i --frozen-lockfile`

Test

- `pnpm test` — runs Jest tests in `src/index.test.ts`
